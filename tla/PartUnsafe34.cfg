\* Partitioned network, Byzantine weight 34 (34%) = 2*Quorum - W.
\* Split-brain attack: z equivocates final votes (b1 to n1, b2 to n2); each
\* node's election independently reaches quorum on a different fork.
\* Expectation: CrossNodeAgreement is VIOLATED.
SPECIFICATION Spec

CONSTANTS
    Blocks = {b1, b2}
    Quorum = 67
    Nodes = {n1, n2}
    InitBlock = b1
    NoBlock = none
    SyncDelivery = FALSE

CONSTANT Reps <- RepsW
CONSTANT Weight <- Weight34
CONSTANT Byzantine <- ByzZ

INVARIANTS
    CrossNodeAgreement
