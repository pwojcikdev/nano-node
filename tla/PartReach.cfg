\* Non-vacuity on the safe (33%) instance: a node CAN confirm.
\* Expectation: NoDecision is VIOLATED (some node reaches a decision), so the
\* clean PartSafe33 result is not vacuous.
SPECIFICATION Spec

CONSTANTS
    Blocks = {b1, b2}
    Quorum = 67
    Nodes = {n1, n2}
    InitBlock = b1
    NoBlock = none
    SyncDelivery = FALSE

CONSTANT Reps <- RepsW
CONSTANT Weight <- Weight33
CONSTANT Byzantine <- ByzZ

INVARIANTS
    NoDecision
