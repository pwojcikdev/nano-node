\* Unit weights, W=6, Quorum=4 (q=2/3). Byzantine weight = 1 (16.7%).
\* 1 < 2*Quorum - W = 2  => safety holds. Fully enumerated over all splits.
SPECIFICATION Spec

CONSTANTS
    Blocks = {b1, b2}
    Quorum = 4
    ByzantineActive = TRUE

CONSTANT Reps <- RepsUnit
CONSTANT Weight <- WeightUnit
CONSTANT Byzantine <- ByzOne

INVARIANTS
    TypeOK
    CorrectAtMostOneFinal
    DecisionSound
    Agreement
    AtMostOneCommittable
