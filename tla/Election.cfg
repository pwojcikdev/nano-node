\* Safe model: honest reps (no equivocation). All invariants must hold.
SPECIFICATION Spec

CONSTANTS
    Reps = {r1, r2, r3}
    Blocks = {b1, b2}
    Quorum = 2
    InitBlock = b1
    NoBlock = none
    AllowEquivocation = FALSE

CONSTANT Weight <- UniformWeight

INVARIANTS
    TypeOK
    DecidedStable
    DecidedHadFinalQuorum
    SafeUniqueFinal
