\* Liveness threshold: omission adversary. Byzantine weight = 34 WITHHOLDS
\* (ByzantineActive = FALSE), leaving correct weight 66 < Quorum 67.
\* Expectation: NothingCommitted HOLDS for all reachable states -> the chain
\* stalls. Demonstrates that > (1-q)*W ~ 33% withheld stake halts liveness.
SPECIFICATION Spec

CONSTANTS
    Blocks = {b1, b2}
    Quorum = 67
    ByzantineActive = FALSE

CONSTANT Reps <- RepsW
CONSTANT Weight <- Weight34
CONSTANT Byzantine <- ByzZ

INVARIANTS
    NothingCommitted
